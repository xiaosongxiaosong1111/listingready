# ListingReady 工作交接 / Task Handoff

最后更新：2026-07-16  
参赛队伍：小宋1021队（个人参赛）  
赛道：AI 智能上新  
GitHub（私有仓库）：https://github.com/xiaosongxiaosong1111/listingready  
分支：`main`  
功能基线提交：`d42e9f9 feat: use real organizer photos`

## 1. 项目目标

ListingReady 把中文商品资料和真实照片整理成可检查的 Amazon 美国站英文 Listing，包括标题、五点描述、详情描述和搜索词，并在发布前提示缺少参数、事实不一致和潜在夸大表达。

当前演示商品是白色窄型抽屉式桌面收纳盒。首版只做一个平台（Amazon US）、一个 SKU 和一个完整演示流程，避免个人参赛项目过度开发。

## 2. 已经完成

- 可运行的 Vinext/React 前端页面。
- 已接入 3 张用户拍摄的真实商品照片：
  - `public/product/scene-horizontal.jpg`
  - `public/product/scene-stacked.jpg`
  - `public/product/scene-drawers-open.jpg`
- 页面展示英文 Listing 草稿、质量检查结果和参数待确认提醒。
- 支持复制 Listing 和下载 JSON 上新包。
- README 已提供中英文说明和本地运行命令。
- 自动测试验证页面文案、真实照片路径和禁止使用的虚构参数。
- 2026-07-16 已修复本机预览服务漏掉 `/product/` 路由造成的图片 404；该预览服务是办公电脑的本地绕过方案，不属于仓库代码。

## 3. 真实照片能够证明什么

可以确认：

- 商品为白色。
- 每个盒体是独立单格抽屉结构，正面有半圆形开口。
- 照片展示了横向排列和垂直叠放场景。
- 抽屉可用于收纳笔、卡片、钱包和其他桌面小物。

尚不能确认：

- 材质（不能仅凭照片写 PP、ABS 或塑料）。
- 外部尺寸和抽屉内部尺寸。
- 单个重量、承重和稳定性。
- 每套销售数量。
- 是否存在卡槽或锁定式堆叠结构。
- 采购成本、售价和毛利。

参数确认前，不要在商品文案中使用 `plastic`、`modular`、`stackable design`、具体尺寸、具体承重或固定套装数量等未经证实的描述。可以说“照片展示垂直叠放”，不能说“已验证可锁定堆叠”。

## 4. 当前 AI/API 状态

已接入赛事 Token Plan 的 OpenAI 兼容接口。`/api/generate-listing` 在服务端调用 `qwen3.6-flash`，并要求模型只使用卖家输入的事实；前端不会接触 API Key。密钥位于本地 `.env.local` 或部署平台的 Secret 配置中，禁止写入源码、README、提交记录或浏览器端代码。

当前本地运行环境可能不允许 Worker 进程访问外部模型服务；专属 Token Plan 地址和密钥已通过独立最小请求验证。部署后需在托管平台配置同名 Secret，再从页面完成一次端到端验证。

## 5. 在另一台电脑继续

要求：Node.js 22.13.0 或更高版本。

```bash
git clone https://github.com/xiaosongxiaosong1111/listingready.git
cd listingready
npm install
npm test
npm run dev
```

常用检查：

```bash
npm run lint
npm run build
npm test
```

当前测试基线：2 个测试全部通过。Lint 只有 `<img>` 性能建议和已有 PostCSS 风格警告，没有错误。三张照片体积较小，MVP 暂不增加图片优化依赖。

## 6. 下一步任务（按优先级）

### P0：补齐真实商品资料

请用户测量或确认：

1. 单个盒体外部宽 × 深 × 高（厘米）。
2. 抽屉内部宽 × 深 × 高（厘米）。
3. 材质，以包装、购买记录或供应商资料为准。
4. 单个重量、合理承重以及垂直叠放是否稳定。
5. 是否有堆叠卡槽或锁定结构。
6. 计划按 1 个、2 个还是 4 个一套销售。
7. 采购成本和计划售价。

建议补拍：白背景正面、45°侧面、完全打开、多个垂直叠放、带尺子的尺寸照片。

### P1：更新可发布前的 Listing 草稿

- 把确认后的参数写入表单和英文 Listing。
- 保留“已验证事实”和“AI 推断/待确认字段”的区分。
- 增加针对最终标题、五点描述和 JSON 的测试。

### P1：接入百炼

- 先阅读赛事提供的 ModelRouter/API 文档，确认认证方式和可用模型。
- 用服务端接口代理请求，前端不得接触密钥。
- 约束模型只能使用用户确认过的商品事实。
- API 失败时显示可理解的错误，并保留当前本地草稿作为演示降级方案。

### P2：比赛材料

- 完成初赛 Idea 文档：用户痛点、方案流程、技术架构、创新点、商业价值和开发计划。
- 准备 3 分钟 DEMO：输入中文资料 → 生成 Listing → 发现缺失参数 → 修正 → 导出。
- 准备决赛演示稿和一分钟项目介绍。

## 7. 发布前注意事项

- 当前仓库应保持私有。真实照片中存在家庭环境、第三方商品标签和私人物品；公开项目前应重新拍摄干净背景商品图，或裁剪/模糊无关内容。
- 不要伪造商品参数、市场数据、用户数量、转化率或模型效果。
- 未经用户明确同意，不要公开部署、修改仓库可见性或提交任何 API Key。

## 8. 给下一台电脑上 Codex 的接手指令

```text
请先完整阅读 README.md 和 HANDOFF.md，然后运行 git status、npm test。
这是小宋1021队参加 AI+跨境黑客松的 ListingReady 项目，方向是 AI 智能上新，演示商品为白色窄型抽屉式桌面收纳盒。
严格区分照片已证明的事实和待确认参数，不得自行编造材质、尺寸、重量、承重、套装数量或堆叠锁定结构。
当前第一优先级是根据用户提供的测量数据更新 Listing；百炼 Key 获得后再接入服务端 API。
所有改动先测试，再提交；禁止把密钥提交到 Git。
```
