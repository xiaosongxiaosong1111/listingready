# ListingReady 工作交接

> **当前基线 2026-09-14：** 先读 [收尾验收与交付](docs/2026-09-14_收尾验收与交付.md) 及 [提交前操作单](docs/复赛提交前操作单.md)。规则/提示词已升 v6，新增 Markdown 导出，72 项测试通过。下面全部为历史交接，涉及旧线上版本、日期和阻塞的描述不得作为当前状态。用户复核、图片方案、联系人/期限、PDF/视频、评委访问仍待处理。

> 2026-09-11 补记：v5 已发布为私有版本 3（源码 379b2c6），68 项测试通过；最新成功线上回执及额度中断点见 [发布记录末尾](docs/2026-09-10_定时续做与发布记录.md)。浏览器已恢复访问，最后一笔页面生成的结果尚未读取。一次性定时任务因重复触发已暂停，不自动继续调用。

最后更新：2026-09-10 凌晨。优先阅读 [定时续做与发布记录](docs/2026-09-10_定时续做与发布记录.md)：68 项测试通过，私有版本已更新，仍有 GitHub 访问和浏览器登录阻塞；以下 9 月 9 日条目为之前的验证基线，不替代最新记录。

## 从这里接手

1. 阅读 [最新续接检查点](docs/2026-09-09_晚间续接检查点.md)，其中晚间追加记录优先于当天早期快照。
2. 阅读 [原详细任务日志](docs/ListingReady_复赛开发任务日志.md)，用最新验收证据解释勾选，不把历史计划当作当前实现。
3. 查看 Git 状态，保留未提交修改，不重置工作区。工作目录为本文件所在仓库，分支 main，原仓库为 https://github.com/xiaosongxiaosong1111/listingready 。
4. 用户安排北京时间 2026-09-10 01:00 在当前任务续做一次。当前继续工作不取消该任务；到时只处理仍未完成项。

## 实现及最新验证

通用 13 项事实 → 已确认且有来源的事实隔离 → 比赛文本模型 → JSON 解析 → 独立风险检查及最多一次修订 → 人工改稿/补事实 → 新旧版本对照 → 服务端复检 → 草稿或人工复核包。

支持多品类；收纳盒与厨房毛巾是案例，不是仅支持的商品类型。场景图另走已授权参考图编辑链路，不能当成图片识别或自动上架。

- 最近 66 项测试通过（36 核心、15 工作台、11 图片、4 生产 Worker）；类型检查、构建通过。
- 10 份旧真实回执按最新规则做 30 次 HTTP 检查，0 次模型调用。
- 浏览器实测待确认材质/尺寸不进入输出，确认后正确生成并保留旧版；风险由 3 个待复核降为 0。
- 包装百分比误报、无依据耐用/性能词和常见否定证据已增加回归测试；不是完整语义审核。
- 图片加载成功前禁止勾选复核；纯逻辑与生产路由边界测试通过，新增加载事件交互仍待浏览器实测。

## 未完成，不得报为通过

- 图片 v3 / Pro 实测仍改变标签和视角。用户未明确选择“AI 背景 + 原始实拍区域”替代方案，不擅自替换。
- 内置浏览器虚拟剪贴板没有读到复制内容；未拿到实际下载文件。已提供持久保存链接和完整 JSON 预览，但不等于实际粘贴/落盘验收。
- 新版私有部署和部署后验收尚未完成；线上仍为 9 月 7 日旧版，且只允许本人访问。
- 最终复赛 DOCX/PDF、3–5 分钟视频、联系人信息及评委访问方式未完成。已备 [提交内容草稿](docs/复赛提交内容草稿.md) 和 [演示脚本](docs/复赛演示脚本与验收清单.md)。

## 开发与安全

保留 Vinext / React / Cloudflare Worker 架构，无数据库。Node.js 24；API 为 generate-listing、check-listing、export-listing、generate-scene 四个路由。常规检查使用 package.json 中 typecheck、test、lint；默认测试不消耗模型额度。

当前 Windows 若 npm 不可用，使用已安装的 Node 运行 node_modules 中的对应 CLI，不必重新安装整个项目。实际运行路径、回执位置和现场状态见最新检查点。

密钥仅在忽略的 .env.local 和对应托管 Secret 中。不得输出、提交密钥；提交前运行 scripts/check-secrets.mjs（需要私有本地环境，只输出计数）。不要自动消费重置券、购买额度、变更访问权限或提交赛事。

用户已授权真实比赛 API 验收，但只做有价值的有限调用。图片外传授权范围仅为 public/product/scene-horizontal.jpg 及已确认商品名称、颜色、结构。遇额度、429、鉴权失败停止模型调用并记录；不要把一般网络故障误判成余额耗尽。
