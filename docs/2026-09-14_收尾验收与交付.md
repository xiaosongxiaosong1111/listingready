# ListingReady 收尾验收与交付

日期：2026-09-14。本文是当前验收基线，优先于早期任务日志中的历史状态。未列为通过的事项不能在路演中宣称完成。

## 本轮实际完成

1. 新增 Markdown 审阅报告。使用与 JSON 相同的服务端复检结果，保留当前英文文案、全部 13 项事实及状态、来源、排除原因、全部风险和图片复核声明。正文转义 HTML 和 Markdown 控制符，不把商品内容当作可执行标记。机器可读数据及生成记录仍以 JSON 为完整版本。
2. 修复“抽拉取物”被扩写成“快速取物”以及“不影响周围物品”的遗漏。新规则要求速度声明有已确认的对应证据；本版保守拦截无干扰取物保证。
3. 扩展颜色选择承诺检查，覆盖真实生成中的 collection includes … variations 和 available finishes。颜色列表不自动证明套装组成或买家可选款式。
4. 提示词与规则升级到 v6。当前提示词为 `facts-only.v6.2026-09-14`，规则为 `amazon-us-non-media-2026-09-14.v6`。图片提示词保持 v3，未宣称解决保真。
5. 68 项核心/工作台/图像测试及 4 项生产 Worker 测试通过，共 72 项；类型检查和最终生产构建通过。秘钥检查覆盖 95 个当前文件、154 个可达 Git 对象，无密钥命中。计数为本轮提交前扫描快照。

## 真实接口和浏览器验收

- 21:32 北京时间：专属文本接口最小预检 HTTP 200，说明此时 Key 可调用，不代表图像配额或未来额度保证。
- 本地初次页面请求因连接问题失败，没有误报余额耗尽，也没有自动反复扣费。取得网络权限并重启本任务的本地服务后生成成功。
- 收纳盒：v5 真生成 1 次物理调用，28.574 秒、6923 tokens。人工发现取物速度和无干扰效果扩写；v6 重检产生 3 项阻断、9 项待复核、3 项通过。这是发现问题并修复的反例，不是合格文案。
- 毛巾：v6 真生成 1 次物理调用，24.266 秒、6549 tokens，无格式修订。人工发现颜色变体措辞；补充规则后重检出现 2 项待复核。人工删除变体承诺并改写描述后，当前规则为 0 阻断、0 待复核、5 通过。这个数字不是语义准确率。
- 实测修改文案后旧导出链接消失、人工复核包按钮禁用；重新准备的 JSON 与改稿内容一致，保留最新规则和未解决风险。
- Markdown 预览长 4162 字符，保留草稿标识与全部 3 项阻断；保存链接正确使用 `.md` 文件名。
- 浏览器宽度 624 像素时页面内容宽度同为 624，无横向溢出；导出区截图可读。没有声称本轮覆盖所有屏幕、200% 字号或全部无障碍场景。
- 复制按钮返回成功提示，但浏览器工具的剪贴板读取为空；下载点击后，未在 Downloads 找到对应文件。因此实际粘贴和落盘仍需用户确认，不能仅凭按钮提示判定通过。
- “我已人工复核”代表用户声明。代理尝试勾选被安全审核阻止，未绕过；用户必须核对并亲自确认。服务端未复核时拒绝导出由自动测试覆盖，不等于用户声明已成立。
- 本轮没有图片生成调用；不重新上传未授权素材。

## 交付物导航

- `README.md`：产品能力、运行、接口、模型配置与安全边界。
- `docs/ListingReady_复赛开发任务日志.md`：最初详细规划；勾选为历史记录，应结合本文使用。
- `docs/复赛提交内容草稿.md`：可转填官方 Word 模板的项目正文。
- `docs/复赛演示脚本与验收清单.md`：4 分钟讲解和录制步骤。
- `docs/复赛提交前操作单.md`：队长在提交前需要完成的最后操作。

## 外部阻塞与安全边界

1. GitHub：9 月 14 日只读检查仍失败，凭据管理器无法使用 wincredman，且非交互环境不能索取用户名。不能宣称已推回原 GitHub。不要公开新仓库或改写历史来绕过；修复连接后正常推送。
2. 在线访问：本轮开始时站点为私有版本 3，仅 1 名允许用户，0 名外部访客。浏览器进入登录页，未登录成功；未扩大权限。后续发布结果追加在本文末尾。
3. 图片：既有参考图编辑仍改变标签或视角。待用户决定是否接受保留原照片的营销排版备选，不擅自改变交付目标。
4. 官方 Word 模板存在，但文档技能要求的打包渲染器失败：缺少 LibreOffice soffice.exe，依赖包内也未找到。没有冒用桌面办公软件或未经检查输出正式 DOCX/PDF。本轮交付可转填的 Markdown，不称正式 PDF 已完成。
5. 未提供电话、邮箱、完整队员名单；9 月 15 日模板截止与原公告 9 月 13 日不一致，必须向主办方确认。当前已是 9 月 14 日，不能再用过去的内部目标当未来计划。
6. 没有最终视频、评委访问确认或赛事提交。不得自动提交、开放网站、新增评审账号或暴露 API Key。

## 明确完成定义

代码阶段：现有文本闭环可用且有真实验收；不是成熟商用多租户服务。提交阶段：正式 PDF、3–5 分钟视频、评委可访问入口、联系人、截止确认和用户复核均齐备后，才能说复赛交付完成。图片如继续列为核心成果，必须另有保真通过证据。

## 发布结果

私有站点于 2026-09-14 发布新版，地址为 `https://listingready-demo.coral-rose-4718.chatgpt.site`。发布保持原有本人私有访问，没有新增外部访客或开放公开权限。原 GitHub 推送仍受本机凭据连接阻挡，不能把 Sites 源码备份描述为 GitHub 已更新。
