# ListingReady 凌晨验收记录

日期：2026-09-09，北京时间 03:00 起的一次续跑。用户要求等凌晨再跑；本轮先检查可用性，再执行有限验收，不购买额度、不切换账号、不改变访问权限。

## 1. 结论与发布状态

- 文本结构化输出验收完成：连续 10 次逻辑请求成功，最终有效结构 10/10。
- 图片接口可用，但 v2 仍改变商品结构，质量不合格，未勾选外观复核。
- 61 项自动测试、类型检查、生产构建通过。浏览器验证了部分重要流程，但完整验收尚未完成。
- 新版仍保留在本地，未提交、未推送、未部署。原私有线上版没有被替换，访问权限没有改变。
- 未生成或提交复赛 PDF、视频或正式比赛材料，不宣称项目达到原详细日志的全部完成定义。

## 2. 本轮真实调用账目

### 首次可用性检查

`scripts/live-preflight.mjs` 为新增的显式授权检查脚本。需要 `RUN_LIVE_TESTS=1` 和本地环境密钥，只向批准的 Token Plan 专属文本地址请求极短响应，不打印请求头或上游正文。2026-09-09 03:01:38 返回 HTTP 200。本轮没有收到额度不足、429 或鉴权失败；这不证明套餐余额或重置周期。

### 连续 10 次文本测试

运行 `scripts/live-stability.mjs`，两种案例交替。原始完整结果仅保存在 Git 忽略目录：`outputs/live-stability-2026-09-08T19-05-41-587Z.json`。文件名用 UTC，对应北京时间凌晨。

| 次序 | 案例 | HTTP | 物理请求 | 模型耗时 ms | 返回 token 数 | 格式重试 | 风险修订 | 阻断 / 待复核 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 收纳盒 | 200 | 1 | 20667 | 5205 | 0 | 0 | 0 / 9 |
| 2 | 厨房毛巾 | 200 | 1 | 20443 | 5651 | 0 | 0 | 0 / 0 |
| 3 | 收纳盒 | 200 | 2 | 32677 | 9240 | 0 | 1 | 0 / 10 |
| 4 | 厨房毛巾 | 200 | 1 | 28039 | 7139 | 0 | 0 | 0 / 0 |
| 5 | 收纳盒 | 200 | 1 | 22829 | 5455 | 0 | 0 | 0 / 9 |
| 6 | 厨房毛巾 | 200 | 1 | 25496 | 6865 | 0 | 0 | 0 / 0 |
| 7 | 收纳盒 | 200 | 1 | 25434 | 5815 | 0 | 0 | 0 / 10 |
| 8 | 厨房毛巾 | 200 | 1 | 23613 | 5263 | 0 | 0 | 0 / 0 |
| 9 | 收纳盒 | 200 | 2 | 44357 | 11082 | 0 | 1 | 0 / 10 |
| 10 | 厨房毛巾 | 200 | 2 | 41986 | 11422 | 0 | 1 | 0 / 0 |

使用模型 `qwen3.6-flash`，提示词 `facts-only.v3.2026-09-08`。共 13 次物理请求，3 次风险修订，0 次格式失败重试、0 次上游重试。所有收纳盒结果仍有待确认事实或重复搜索词等警告，不能导出复核包。5 次完整毛巾资料的结果满足当前程序规则门槛；**没有因此替用户确认文案真实性**。

另有 1 次浏览器毛巾生成，服务端 HTTP 200，约 51.8 秒，结果成功显示。其物理请求数及 token 数本轮没有另存，不并入上表，也不编造汇总。

### 真实图像 v2

- 通过既有浏览器的“载入收纳盒案例参考图”，使用用户已经授权的 `public/product/scene-horizontal.jpg`，确认上传同一比赛接口。
- `qwen-image-2.0`，提示词 `reference-preserving.v2.2026-09-08`，背景“居家办公桌，柔和自然光”。一次请求 HTTP 200，页面显示约 3.8 秒，结果图片加载成功。
- 原图为四个抽屉横排在置物架下。新图改变为桌下竖向叠放的抽屉柜，排列、比例与场景关系发生变化。
- **不合格。** 没有勾选外观复核，没有作为合格图片下载、发布或纳入正式材料。本轮不继续反复生成或换模型抽取“看起来成功”的结果。
- 未保存生成图片二进制备份；工具可视截图显示过原图/结果对照，但未保存仓库截图文件。不要声称已有可长期使用的失败图附件。
- 仅加强文字 Prompt 并不能保证外观保持。页面说明现改成明确的“尝试改变背景与光线；模型仍可能增减部件或改变商品结构”，避免做出已经证伪的保证。

## 3. 浏览器验收：通过与待查分开

### 本轮已验证

1. 收纳盒参考图载入、授权门禁、真实生成、原图/结果并排显示、人工复核未勾选状态。
2. 切换到厨房毛巾后，参考图和生成图清空，不再显示收纳盒载入按钮；场景推荐切换为厨房台面与餐边柜。
3. 毛巾真实生成结果渲染完整标题、Item Highlights、5 条卖点、描述、搜索词，并标为 AI 草稿。
4. 手工把标题改成 `Perfect waterproof towel`，免费重检返回 2 个阻断：无依据防水功能、高风险宣传用语。页面阻止复核包导出。
5. 点击“核对材质”，对应事实折叠区打开，实际焦点位于 `value-material`。
6. 改稿后出现手工改稿基线的完整对照，标题标为变化，其余段落标为未变化。这里只验收手工修改，不冒充补事实后重新生成的对比。
7. 恢复原始标题后重检；点击草稿下载，页面最终提示“已导出草稿、商品事实和完整风险报告”。下载文件内容尚未读取，因此仅记录前端流程成功反馈。
8. 修改材质内容后自动转为待确认，复制草稿、下载草稿、复核包三个按钮全部禁用。
9. 390×844 和 1440×900 临时视口检查：页面内容宽度分别 375、1425，没有页面横向溢出；这不是全页面可访问性或全部移动交互通过。已恢复默认视口。
10. 测试结束重新载入干净的毛巾案例，清除人为测试字段，没有把测试宣称留在用户表单。

### 未通过或尚未完成

- 单段复制按钮显示成功，但浏览器工具读取剪贴板为空，未核实字节一致性。不能判定复制已验收，也不能仅据此断言应用写入失败；需在前台实际粘贴或检查该浏览器的剪贴板桥接。
- 下载草稿未取得落盘文件做完整比对；带图导出、图片备份未验收。
- 没有测试真实模型超时后页面恢复，也没有故意消耗额度触发 429/401。相关有限重试和错误映射已有模拟测试。
- 未完成修改材质/尺寸并重新生成的全链路对照；200% 放大、完整键盘导航与全部移动操作尚未完成。
- 毛巾描述中存在 `reliable performance`、`long-lasting results` 等泛化措辞，现有规则未阻断。不因规则为零就认为所有语义都有来源，正式文案需进一步收紧并人工核验。

## 4. 自动测试、安全与源码

- 本轮运行 36 项核心 + 12 项工作台 + 10 项场景 + 3 项构建 Worker 测试，61/61 通过。
- 类型检查与生产构建通过。构建产生四条 API 路由，包括 `/api/generate-scene`。
- 调整图像说明后再次构建并运行全部 61 项测试，仍全部通过；Lint 为 0 错误、6 个既有图片/默认导出警告。
- 对当前可提交源码与构建产物的 85 个去重文件进行真实环境密钥的内存匹配扫描，0 个匹配。未输出密钥。此扫描不等同于完整 Git 历史或其他密钥扫描。
- 密钥保持在忽略的本地环境文件与既有托管 Secret 中，不写入日志、源码或 Git。
- 当前所有原有未提交改动均保留。本轮新增可用性检查脚本、本验收记录；修改图像能力说明，并同步原计划中有证据的验收勾选。

## 5. 下次续接：不重复已完成的 10 次测试

1. 先读取本文件和原详细 MD 的最新状态，保留 dirty worktree；不重新 clone 或覆盖。
2. 优先处理图片保真路线：与用户商讨更清晰、单主体、无遮挡参考照，或受约束的背景替换方案。新照片发送至外部接口需确认权利及授权。原图的相同授权不扩展到任意照片。
3. 完成前台剪贴板实际粘贴及草稿落盘比对；补事实后重新生成验收至少一次，保存前后两版证据。
4. 收紧无依据性能措辞，补相应回归用例。不要把正则继续堆叠包装成全面语义事实核验。
5. 完成图像质量和导出关键门槛后，重新构建、测试、扫描，按 Sites 技能提交验证后的源码并私有发布至原项目。不要改变访问权限。
6. 后续仍需商业价值证据、正式 PDF、演示视频与备份、评委访问方式、比赛专属套餐后端适用范围确认；不自动提交赛事材料。

技能影响：本轮遵守 Sites 建站/托管流程保留已有架构与本人私有访问。因验收仍有明确未通过项，停止在本地验证与状态保存阶段，没有执行发布，也没有在后台主动打开新的预览窗口。
