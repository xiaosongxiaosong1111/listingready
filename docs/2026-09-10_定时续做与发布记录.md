# 2026-09-10 定时续做与发布记录

北京时间凌晨 01:00，既有一次性任务触发，在原工作目录继续。晚间检查和 66 项测试不重复记为新的成果。

## 源码与部署

- 晚间功能提交：2f393d16f4abc365c3dbdba1d6ff40fd81beefec，含工作台、参考图实验链路、导出预览、66 项测试和复赛 Markdown 材料。
- 已推送到原 Sites 项目的专属源码仓库，不等于推送到用户 GitHub。
- Sites 版本 2 已发布成功，仍为本人私有，Secret revision 1。实际地址：https://listingready-demo.coral-rose-4718.chatgpt.site 。
- 版本 2 ID：appgprj_6a64827bb5b48191b4ba2fe7751c9755~appgver_31303a49fbdc8191b60d14aac6b7c5dc。
- 部署 ID：appgdep_6aa19164ecd081919c27bf776e0d6246。
- 发布前确认 owner、custom、1 用户、0 外部访客、0 用户组。未改变访问权限。
- 原 GitHub 推送失败：本机 Git 登录组件无法使用 Windows 凭据存储；GitHub 连接器读取原私有仓库返回 404。这不证明仓库不存在，也可能是当前连接没有访问权限。需要用户恢复该账号的仓库访问；不新建替代仓库、不强推、不改历史。
- Windows 打包使用现有 Git Bash，在 Bash 内显式设置 /usr/bin、/bin 及已有 Node 路径后，官方 package-site.sh 成功；不要使用系统 WSL 的 bash。

## 线上验证：先有成功，再人工发现遗漏

1. 无模型回放：10 份历史真实结果，30 次线上 HTTP，全部通过。证据 outputs/saved-receipts-check-2026-09-09T17-07-17-541Z.json。
2. 只新增一次真实文本生成：qwen3.6-flash，提示词 v4，25,423 ms，1 次物理调用，5,877 tokens，格式重试 0、风险修订 0。
3. 版本 2 的程序检查返回 5 pass / 0 warn / 0 block；线上重检 200、草稿导出 200、未人工确认的复核包 409。证据 outputs/hosted-smoke-2026-09-09T17-08-50-003Z.json。
4. 人工阅读发现：household cleaning 超出已确认的 kitchen dish drying；color options 暗示可选款式而颜色事实没有说明选择权。因此“零规则风险”不代表完全准确，不能把该输出记为人工验收合格。

## 基于真实反例的 v5 修复

- 规则版本 amazon-us-non-media-2026-09-10.v5，文本提示词 facts-only.v5.2026-09-10。
- 无对应已确认用途时，拦截泛化的 household / general-purpose / multipurpose cleaning；保留真实范围，不从餐具擦干推成一般家务清洁。
- 引用了颜色但没有可选款式证据时，对 color options、select from 等选择承诺提示待复核。单纯颜色列表不报警；明确且有来源的可选款式可用。
- 仍是有限措辞规则，不能覆盖所有语义错误。没有承诺完全消除幻觉。
- 用同一真实回执回放，新规则准确给出 1 阻断 + 1 待复核，无新增模型调用。
- 新增 2 项回归：用途范围及否定证据、颜色列表与可选款式区分。
- 最新 68 项测试全部通过（36 核心 + 17 工作台 + 11 图片 + 4 生产 Worker），类型检查和生产构建通过。
- scripts/verify-saved-receipts.mjs 支持原私有站点的只读访问凭证，仅向固定批准站点发送，禁用重定向；凭证不写文件。
- 新增 scripts/live-hosted-smoke.mjs：明确 opt-in、一次逻辑生成、随后无模型重检/导出验证；失败不自动新增生成。

## 浏览器与图片未完成项

- 浏览器工具初次启动失败，重置后恢复；原 localhost 页面保留旧真实结果。再次复制仍只收到应用成功提示，虚拟剪贴板为空，实际粘贴未验收。
- 线上浏览器进入 ChatGPT 登录页，没有现成登录会话。没有替用户猜邮箱、输入密码或改变认证；需要用户在原页面登录后继续线上浏览器操作验收。
- 导出 JSON 的预览内容已核对，实际下载文件落盘仍未拿到证据。
- 图片 v3 标签和视角问题未解决，本轮未追加图像生成。加载前复核门槛的事件级浏览器检查待完成。
- 图片作为现有 P1 实验功能明确标注保留，本次私有迭代发布不等于完整复赛作品完成。此前“AI 背景 + 保留实拍区域”方案仍待用户明确选择。

## 下一步

1. 将 v5 已验证源码发布到原本人私有站点，并在本记录末尾追加真实结果，不能只写计划。
2. 线上用已保存回执验证新规则实际生效；无必要不再完整重复十次模型生成。
3. 若做 v5 真实验收，单独记录其版本、调用数和人工检查；v3 的 10/10 不能复用为 v5 结果。
4. 用户完成私有站点登录和 GitHub 连接后，补线上浏览器、复制/下载和原仓库推送。
5. 完善最终复赛 PDF 与视频。联系人信息和评委访问方式仍需用户确认，未自动提交赛事。

只需用户处理真实阻塞，不要求购买服务器、购买额度、消费重置券或公开密钥。一次定时唤醒已执行，不另外安排持续轮询。

## 最终发布结果补记（2026-09-11 整理，优先于上方中途状态）

- v5 修复已完成私有发布，不再是待发布计划。
- 线上源码提交：379b2c6459d11748c57b92e5ab27904c0da0a6de；本地及原 Sites 源码仓库均已保存，用户 GitHub 尚未推送成功。
- Sites 版本 3：appgprj_6a64827bb5b48191b4ba2fe7751c9755~appgver_67fef46284548191bf0ecbec5a5e93ea。
- 部署 appgdep_6aa1940a68708191ba47596fbfaab936 已返回 succeeded，Secret revision 1，仍是原本人私有地址。
- 线上 v5 对旧反例的 3 次无模型接口验证通过：检查 200，草稿 200，未人工确认的复核包 409；风险为 1 阻断 + 1 待复核。回执：outputs/saved-receipts-check-2026-09-09T17-15-28-431Z.json。
- 新增一次 v5 真实线上文本请求：qwen3.6-flash，25,332 ms，1 次物理调用，6,658 tokens，0 格式重试、0 风险修订，5 pass / 0 warn / 0 block。随后重检、草稿导出和未复核拦截均通过。回执：outputs/hosted-smoke-2026-09-09T17-16-09-317Z.json。
- 人工阅读该 v5 回执，未见上一份反例中的 household cleaning 和可选颜色承诺；不因此声称全面事实准确或合规通过。
- 上方“浏览器必须由用户登录”的中途阻塞后来解除：同一浏览器已实际打开新版工作台，并加载厨房毛巾案例。未变更权限或输入用户密码。
- 页面又发起一次真实生成以验收浏览器流程，已看到“正在生成”提示；随后读取结果的动作被自动审批层以 Codex 使用额度限制拒绝。没有绕过该拒绝，也没有把这个未读到的结果标成成功。上游请求可能已经完成或计费，下一次应优先读取现有页面，不直接重新生成。
- 因此本次有 2 次已保存成功的逻辑生成（v4 与 v5 各一次），另有 1 次浏览器请求已发出但结果未验收；未新增图像调用。
- 68 项自动测试与构建仍是最近有效验证基线；源码更新后只剩回放脚本兼容单份回执的本地小改动，以及本次日志补记尚未提交。不要重置这些改动。

## 2026-09-11 重复触发处置

原任务明确只在 9 月 10 日执行一次，但 9 月 11 日 01:00 又收到同一调度。检查发现任务仍为 ACTIVE。为遵守一次性执行意图，已通过应用工具将 listingready 任务设为 PAUSED，保留原名称、提示词、时间配置与所属任务；没有手改调度文件，没有新建定时任务。

今天只进行暂停和状态补记，没有继续比赛 API 调用，也没有消费重置券。需要再次自动续做时，由用户重新指定时间。

下一次人工继续的顺序：先读已有浏览器请求结果 → 验收改稿/导出与实际粘贴/落盘 → 恢复原 GitHub 访问并推送 → 确认图片方案与最终参赛材料。图片保真、最终 PDF/视频、评委访问方式仍未完成。
