# ListingReady 晚间续接检查点

记录时间：2026-09-09，北京时间。用户要求保存状态，9 月 10 日凌晨 01:00 在当前任务继续一次。本文优先于旧的“明日”记录；不要重复此前已完成的测试来消耗额度。

## 已完成，但尚未提交 / 推送 / 部署

- 最近生产构建及 64 项自动化测试通过。最终类型检查、lint 仍应在收尾时重跑；不要把早前检查当作最新改动的检查。
- 文本提示词更新为 facts-only.v4.2026-09-09，规则为 amazon-us-non-media-2026-09-09.v4。加强无依据耐用性、性能措辞和否定证据检查，但只是保守规则，不是完整语义判定或平台合规认证。
- 修复真实生成暴露的包装数量误报：4-pack of 100% cotton 不再把 100% 误判为 100 件；新增回归测试，真正 Pack of 100 仍检查数量。
- 浏览器真实验证厨房毛巾案例：材质、尺寸设为待确认时不生成这些参数；确认后再生成正确包含 cotton / 18x24，结果 0 阻断、0 待复核。前后事实变化和版本对照可见。
- 导出改为“准备文件 → 保存 JSON 文件”持久链接，并提供完整 JSON 预览。准备成功不等于文件已落盘。浏览器读回 JSON 与界面一致、未含密钥；当前自动化环境没有拿到实际下载文件，不能将下载落盘标记通过。
- 复制接口返回成功，但内置浏览器虚拟剪贴板为空，跨界面粘贴未能验证；不能据此断定产品复制功能失败，也不能标记验收通过。
- 修改商品身份时切为自定义案例，避免展示固定收纳盒图片；图片区标为实验功能、逐图复核。
- 10 份已保存真实回执做了 30 次无模型 HTTP 回放校验。该次回放早于最后包装误报修复，可用脚本重新验证最新规则，不消耗模型额度。
- 扫描当前源码、构建与可达 Git 历史，最近结果 currentFiles=90、reachableObjects=92、密钥命中 0。最后提交前再扫描一次。

## 图片：仍未通过外观保真验收

- v3 提示词取消强制方图尺寸、加负向提示词、缩短事实输入。仅一次 qwen-image-2.0-pro 真实试验，默认模型未全局切换。
- 四个横排抽屉保住了，但标签乱码、视角/比例及周边道具仍变化。接口成功不代表可用营销成品；保持未复核状态。
- 本地证据（outputs 被 Git 忽略）：scene-2026-09-08T23-43-59-701Z.json 和同名 PNG。
- “AI 生成背景 + 原始商品照片保持不变”的替代方向已提问，但用户未明确选择。不得静默替换成不同交付方案。
- 仅 public/product/scene-horizontal.jpg 及已确认商品名称、颜色、结构获明确外传测试授权。不要上传新的任意图片。

## 凌晨按这个顺序继续

1. 读取本文、原详细任务日志、HANDOFF 和现有技能；检查 Git 状态，保留全部当前修改。当前大量 9 月 8–9 日改动未提交，不能重置工作区。
2. 检查本地服务是否仍在 localhost:3000。上次 dev 会话 49442；不要凭会话编号假定服务仍活着，也不要停止无关进程。
3. 做最新类型检查、lint、生产构建、64 项测试；使用 scripts/verify-saved-receipts.mjs 回放旧回执校验新规则。具体输入先读脚本和 outputs，勿盲跑付费脚本。
4. 有针对性补齐文本生成、改稿、风险拦截、旧结果失效、导出预览与移动布局检查。已验证环节不必反复生成。当前 v4 提示词没有重新连续 10 次验收，旧 v3 的 10/10 不能归为 v4。
5. 更新 README、HANDOFF、原详细任务日志和工作状态，使“已实现 / 已测试 / 已部署 / 待确认”一致；旧 README 存在图片功能未实现的历史表述，需要区分线上旧版和本地新版。
6. 完成安全扫描，审阅改动并提交、推送用户原仓库。密钥仅保留忽略的 .env.local / 已有托管 Secret，任何日志、源码、构建和 Git 历史都不得包含明文密钥。
7. 按 Sites 技能进行原站点的私有部署和部署后回归，先满足相应发布门槛。保留图片实验限制，不宣称整体成熟；不得改变访问权限或另建公开站点。若图片验收或新方案需要用户决定，先保存其他进展并报告具体问题。
8. 继续完善 docs/复赛提交内容草稿.md，可准备 3–5 分钟演示脚本及复赛验收清单。无实际数据不得编造用户测试、转化率、效率提升或商业订单。

## 赛事材料与尚缺选择

- 已按用户原始复赛 DOCX 模板提取要求并写出 Markdown 草稿；未制作最终 DOCX / PDF / 视频。
- 模板 deadline 为 9 月 15 日，原公告为 9 月 13 日，须向主办方确认；内部按 9 月 13 日准备。
- PDF 文件名：小宋1021队_ListingReady_复赛作品.pdf。
- GitCode 在模板中是可选项，未创建或授权评委；现有 Demo 仍只有本人访问，评委访问方式未确认。
- 团队手机号、邮箱、成员资料仍需用户提供；不在交接文档内记录密钥。

## 执行与安全边界

- 用户已授权比赛 API 真实调用，但按验收必要性使用，不以耗完额度为目标。遇到明确额度不足、429 或鉴权失败停止后续模型调用，不反复重试、不购买、不换账号；普通网络故障与余额不足分开判断。
- 不自动消费 Codex 重置券，不自动提交比赛、不购买服务器、不扩大站点访问范围。
- 当前线上旧版：https://listingready-demo.coral-rose-4718.chatgpt.site/ 。最近查询仍为本人私有（1 用户），新改动尚未部署。
- 定时续做为一次性唤醒，不意味着可以保证额度在 01:00 恢复。环境或额度不允许时，记录阻塞并通知，不声称已开发。
- 只在有实质进展、完成、失败或需要用户决定时通知；没有变化不要重复发送状态。

## 晚间继续开发追加记录（优先于上方快照）

- 用户随后要求继续，凌晨 01:00 的一次续跑不取消；定时执行时从本节和 Git 当前状态接手，不重复已做工作。
- 类型检查通过；lint 为 0 错误、6 个已有建议（图片加载优化和配置默认导出），不影响编译。
- 最新规则回放：outputs/saved-receipts-check-2026-09-09T13-01-39-535Z.json，10 份回执、30 次 HTTP、0 次模型调用，全通过。
- 新修复：图片尚未加载、加载失败、旧图片、请求进行中或未确认授权时，均不能勾选外观复核。绑定当前图片 ID，不能沿用上一张图的加载状态。新增纯逻辑回归；浏览器 onLoad/onError 事件仍待专项实测。
- 新增生产 Worker 图片路由测试：错误媒体类型、跨域、非法 JSON、超限请求均在模型调用前被拦截，响应 no-store / nosniff。
- 最新生产构建、类型检查和 66 项测试通过（36 核心 + 15 工作台 + 11 图片 + 4 生产 Worker）。
- 已更新 README、HANDOFF、原详细任务日志的当前实现/测试边界；已新增 docs/复赛演示脚本与验收清单.md。
- 本次续做未新增真实模型调用，没有为消耗额度重复旧的十次生成。图片保真、真实复制/下载、部署与最终参赛材料仍未完成。
